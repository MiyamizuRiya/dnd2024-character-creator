# 修复记录(2026-09-25)

> 依据:《网站测试问题清单(2026-09-23)》+《问题解决方案(2026-09-23)》,按建议顺序执行。
> 执行环境:电脑 B(接入 Gitee 双机同步当日),git 2.55 + Node 24(已补装)。
> 验证方式:本地 HTTP(127.0.0.1:8797)+ 浏览器真机驱动(逻辑注入、状态断言);搜索阈值调整附真实数据离线复现(法术 391 / 物品 417 全量)。

## 一、已修复(对照 9-23 清单编号)

| # | 问题 | 修复要点 | 真机验证 |
|---|---|---|---|
| 1(2+N1) | 子职三连:面板不刷新/降级残留/1级非法选 | `pickLevel` 补 `renderSubclassPick()`+跨3级 toast+**降级清除 S.subClass**;隐藏分支清空 `innerHTML`;`pickSubclass` 加 `(S.level||1)>=3` 守卫 | ✅ 1级选被拒;3级选→降回1级后面板隐藏、内容清空、S.subClass=null |
| 2(1) | A2 天赋法术同步失效 | build.js 合集模板加**父页消息中继**(`dnd:granted`/`dnd:requestGranted` 转发给兄弟 iframe),兼容 http 与 file:// | ✅ 合集内提夫林(炼狱)→ 奇术/火焰箭/炼狱叱喝 自动入法术书 |
| 3(N2/N3) | ASI 显示字段错 + 未选背景区块消失 | `renderSheet` 按 `asiMode` 分支渲染(`plus2` / `plusA`+`plusB`);【属性提升/专长】【专长子项】移出 `if(bg)` 独立渲染 | ✅ 4级战士无背景仍显示,`+1(力量) +1(敏捷)` / `+2(力量)` 正确 |
| 4(P1-3) | 施法职业的魔法学徒被忽略 | `spellCantripCount` 各段 +2 戏法;`classSpellList` 并入 MI 职业戏法(**一环不进主列表**,避免跨职业占准备位);新增 MI 一环常备下拉(`setMiSpell`→`alwaysPreparedSpells` 常备,不占位);`resetAll` 清 `miSpellChoice` | ✅ 法师+侍僧(魔法学徒(牧师)):戏法 5、书内 6、常备含 MI 一环、DND1 代码含常备 |
| 5(P1-4) | 兼职施法职业无法选法术 | 新增 `castingSegments()` 施法段模型(primaryCaster 重构为其第一段);`classSpellList` 改**全段并集、各段按自身最高环封顶**;`segCantripCount`/`segSpellCount` 按段计算后合计;新增 `classLevelsOf()`(兼职法师可准备数显示修正)。主职业专属加成(结社/圣职/替代风格)只作用于主段 | ✅ 法师2+牧师1:戏法 6、法术 12、牧师一环可选、法师二环正确封顶(法师2级无二环) |
| 6(P2-8+N5) | 熟习未并入角色表 + 健壮缺表 | `renderSheet` 技能行/被动察觉并入 `featSkills`(S.featChoices 展开的合法技能);GENERAL_FEATS 补「健壮」 | ✅ 人类+熟习(察觉/威吓/隐匿)→ 技能行「运动+1、察觉+2、威吓+1、隐匿+1」、被动察觉 12 |
| 7(P2-5) | ☰ 按钮遮挡人物卡顶栏 | `init()` 检测 `window.parent!==window` 加 `html.embedded`;CSS `html.embedded .head-actions{margin-right:130px}` | ✅ 合集内 margin-right=130px 生效 |
| 8(P2-6) | 购点超支不拦截 | `updatePoint` 超支禁用 `#abi-next-btn`;`renderAbi` 非购点法恢复 | ✅ 27 点放行、28 点禁用 |
| 9(P2-7) | 人类技能子选项 undefined | `${op.d!==undefined?op.d:(SKILL_ATTR[op.v]||"")}` 回退属性名 | 代码级修复 |
| 10(P2-9) | 种族子选项未选完静默跳过 | `go('ability')` 前置 `speciesChoicesComplete()` 校验(血系/专长/技能全部必选,比原方案更严:isFeat 也必选) | ✅ 未选被 toast 拦截,选完放行 |
| 11(P2-10) | 法术书角色名不刷新页标签 | `wire()` 加 `#charName` input 监听 → `renderPreview()` | 代码级修复 |
| 12(P2-11) | 「已准备」文案混淆 | 法师(含兼职,按 `classLevelsOf("wizard")`)显示「法术书内 N 个(可准备 M 个)」 | ✅ 随验证4断言 |
| 13(N6) | 置顶不清手动槽位 | 法术书/物品卡 `pinToTop` 各补 `delete state.slotHints[id/uid]` | 代码级修复 |
| 14(P3-12) | 417/419 条数不一 | build.js 入口卡、AI背景说明.md、DMsBestFriend/项目文档.md 三处统一 417 | 构建自检通过 |
| 15(P3-13) | 导出文件名未净化 | 新增 `safeFileName()`(白名单 `[^\w\u4e00-\u9fa5]→_`、截 20 字、空名→未命名),exportProgress/exportText 统一;exportText 补 DOM 清理 | 代码级修复 |
| 16(P3-14) | 全站缺 favicon | 三工具入口 + build.js 合集模板 `<head>` 加 `<link rel="icon" href="data:,">` | ✅ 合集页 link 存在 |
| 17(P3-16) | 物品卡缺「仅消耗品」筛选 | index.html 加 `#consFilter` 开关;app.js filters.cons + getFiltered 过滤 + wire 监听 | 代码级修复 |
| 18(P3-17) | 掷点/数列仅 UI 层防线 | `setArray`/`setRollAssign` 函数级去重校验 + toast | 代码级修复 |
| 19(N7) | 死代码 | 删除 `spellSlotsText()`(约28行,零调用)、`setBgAll1()`(零调用) | 语法检查通过 |
| 20(N8) | 模糊搜索垃圾查询大面积弱命中 | **根因实测**:拼音层字符覆盖率误放行(垃圾查询拼音字母集覆盖过半拼音名,237/391 复现)。修复:`fuzzyScore` 双条件(cov≥0.6 且 sim≥0.4)+ 乱序豁免(字符集相同视为强命中);新增 `pyScore` 拼音层专用(子串或 sim≥0.6,按查询长度归一) | ✅ 离线全量复验:垃圾查询 0 命中;治疗 298→8、火球 286→15、治疗药水 299→30、shuoshanjia 408→2;甲烁闪→闪烁甲(第1)、摩登肯→魔邓肯(第1)等旧功能无回退 |

## 二、方案调整说明(与 9-23 方案文档的差异)

1. **N4(法师子职常备表)方案作废**——按方案去 5echm 抓数据时发现:2024 PHB 法师四子职(防护师/预言师/塑能师/幻术师)3 级特性均为「XX学者」:**由玩家自选两道不高于二环的本学派法术免费入书**(且每获新环阶法术位再免费加一道),**不存在与牧师领域/德鲁伊结社相同的固定常备表**。9-23 清单的"规则核实"有误(预言师 3/5/7/9 级固定法术表在 5echm 原文与 2024 PHB 中均不存在)。**未注入错误数据**;正确实现应是"学者"特性 UI(选学派法术),作为后续需求排期。
2. **P1-4 魔法学徒一环不进主可选列表**(方案原文并入"戏法+一环"):避免玩家把 MI 职业一环选进本职业准备位(违反规则);MI 一环走专用常备下拉。纯魔法学徒(无施法职业)维持原有列表内选择流程不变。
3. **P1-5 采用"方案A-lite"**:不引入 `S.chosenSpellsByClass` 状态模型(避免导入/导出兼容风险),列表为全施法职业并集、计数为各段合计、各段按自身最高环封顶;**未实现按段分别计数约束**(提示文案已注明"请从对应职业列表选择"),完整分职业段 UI 留作后续。
4. **P2-9 校验比原方案更严**:原方案 `ch.isFeat||!!S.speciesChoices[ch.key]` 会放过未选的人类专长,与测试场景("人类专长/人类技能均未选应拦截")矛盾,改为全部必选。
5. **pickLevel 降级清除子职**超出方案三行改动:3 级选子职后降回 1 级会残留非法状态,现随等级<3 一并清除。

## 三、构建与回归

- `node build.js`:合集 2,046,723 bytes,三副本一致,五项内容自检全过;
- JS 语法:冒险者指南(328K 内联)、法术书 app.js、物品卡 app.js、build.js 全部 `node --check` 通过;
- 真机断言 8 组全过(见上表"真机验证"列);
- 环境项(问题 19/20):本机已装 Node 24 与 git 2.55,环境问题不复存在。

## 四、遗留(后续排期)

- 法师子职「学者」特性 UI(自选学派法术免费入书)——替代 N4 的正确实现;
- P1-5 完整分职业段计数约束(方案A 全量);
- 问题 18(法术书偶发冻结):本轮压力场景未复现,维持观察;
- 武僧暗影之道/EK/诡术师常备授予评估(9-23 方案顺带项,未处理)。
