# 修复记录(2026-09-25 · 批次二:五项待修全部落地)

> 依据:本日「应修未修」清单五项(切换职业导航刷新/法师学者/魔法探秘/兼职分段约束/跨来源常备可见),全部当日修复。
> 验证:本地 HTTP + 浏览器真机断言 8 组全过;`node build.js` 三副本一致。

## 修复明细

### 1 · 切换职业后顶部导航不刷新(9-18 第6轮遗留,9-22 重编号遗漏)
- `pickClass()` 补 `renderStepper()`(与 `pickBg()` 对齐),另补 `renderClassFeaturesPick()` 并清空自选法术新状态。
- 验证:野蛮人→法师「法术」步骤即时出现,切走即时消失 ✓

### 2 · 法师子职「学者」特性(规则缺口,9-23 N4 的正确实现)
- 新增 `savantInfo()`:法师四子职 3 级起,**前两道限二环内本学派法术**,此后每获新环阶(5/7/9/11/13/15/17 级)各加一道(≤当前最高环),共 2~9 道。
- 法术步骤渲染「学者法术」下拉组(学派过滤+去重+按序号环阶上限),`setSavantSpell` 写入 `S.savantSpells`;`alwaysPreparedSpells` 以「法师·XX学者」来源并入。
- **建模口径**:规则原文为"免费入书"(书内免费抄录);本工具按常备显示(不占抄录额度、始终准备)近似处理,与魔法学徒一环同口径——法术卡导出/角色表常备区自动可用。
- 验证:预言师 5 级 → 3 道(通晓语言/侦测魔法/预言术)入常备 ✓

### 3 · 吟游诗人·逸闻学院 6级「魔法探秘」
- 新增 `loreSecretInfo()` + 「魔法探秘」两道下拉(牧师/德鲁伊/法师列表,可为戏法),`setLoreSecret` 写入 `S.loreSecrets`,常备来源「吟游诗人·魔法探秘」。
- 验证:治疗真言/法师护甲两道入常备 ✓

### 4 · 兼职施法分段计数(方案A 全量)
- 法术步骤改**按职业列表分段渲染**(奥法骑士/诡术师并入法师段;跨职业魔法学徒单列戏法段;主段替代战斗风格的附加戏法并入主段):各段独立列表、独立上限、独立已选 pills。
- 状态模型:`S.chosenCantripsByClass`/`S.chosenSpellsByClass`(按段键存储),`chosenCantrips/chosenSpells` 降为**派生平铺**(去重保序)——角色表/导出/常备比对/DND1 代码全部零改动兼容。
- 旧档迁移:平铺非空而无分段时,自动全部归入第一段;导入旧进度文件前显式清分段字段防串档;换职业/重置同步清空。
- 角色表法术总览新增「各职业段」进度行(法师 戏法0/3·法术0/8 · 牧师 戏法0/3·法术2/4)。
- 验证:法师2+牧师1 牧师段第 5 道被拦(上限 4)、法师段不受影响 ✓;旧档迁移+导出导入回环 ✓;单职业回归 ✓

### 5 · 跨来源常备法术在法术步骤可见
- 法术步骤新增「跨来源常备」汇总行:不在任何职业段列表内的常备法术(如木精灵游侠的德鲁伊伎俩)以徽章列出(悬停显示来源),不再"建卡时看不见"。
- 验证:木精灵游侠显示德鲁伊伎俩 ✓

## 状态字段变更(供两台电脑 AI 知悉)

`S` 新增:`miSpellChoice`(批次一)、`savantSpells[]`、`loreSecrets[]`、`chosenCantripsByClass`、`chosenSpellsByClass`(后两者为分段真源,平铺数组为派生)。导出格式 `dnd2024-character/v1` 不变,旧档兼容(自动迁移)。

## 遗留更新(对照《规则核对问题清单(2026-09-25)》第三节)

- 遗留 1(逸闻学院魔法探秘)→ **已修**(本批 #3)
- 遗留 2(法师学者)→ **已修**(本批 #2)
- 遗留 3(星辰结社星图条件)/4(5echm 名称不一致)→ 维持原状(记录备查)
