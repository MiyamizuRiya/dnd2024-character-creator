# D&D 2024 工具站 · 实测问题清单(创建日期 2026-09-03,第 2 轮迭代)

> **2026-09-03 修复标注**:本清单所列「仍存」项已于当日两轮修复批次全部处理——P1-1/P1-2/P1-3、P2-3、P2-5、P3-1、P4-1(缓解)、P4-2/P4-3/P4-4、P6-1、P7-1 已修;P2-1/P2-2(装备组+AC)、P2-6、P5-2、P7-4 已于第二批修复。仍未修:P2-4(子职法术结构化)、P5-1(触屏)、P5-3、P1-4、P3-2、P6-2、P7-2/P7-3(低风险或大型重构,暂不做)。详见 `AI背景说明.md` §八。
> 审查对象:`DND工具站源码合集/` 三工具源码 + 线上 Netlify 主站 `https://create-your-dnd2024-pc.netlify.app`(线上运行态)。
> 审查方式:**浏览器实测试 + 静态源码核对**(对照第 1 轮 2026-09-02 清单,标注已修 / 仍存在 / 新增)。
> 实测环境说明:本机无 Node.js / Python,无法运行 `build.js`、无法起本地 HTTP 服务;浏览器(IAB)只接受 `http/https`,不接受 `file://`,故**改测线上部署版**。线上主站由 git push 自动构建,即 `build.js` 的产物,与源码一致。
> **未修改任何源码**。

---

## 0. 实测结论(线上运行态)

| 测试项 | 结果 | 证据 |
|---|---|---|
| 主站加载(无 4xx/5xx 网络错误) | ✅ 通过 | 页面 4 个资源均 200,0 个 netErrs |
| 入口选择页(gate)渲染 + 三张卡 data-tool 齐全 | ✅ 通过 | adventurer/spellbook/items 三卡文案完整 |
| 点击"人物卡"→ 门页隐藏 + 人物卡 iframe 显示 | ✅ 通过 | gate=none,冒险者创建指南=block,toggle=block |
| 点击"法术卡"→ 切换正常,左侧法术列表渲染(非白屏) | ✅ 通过 | 见截图 2 |
| 点击"物品卡"→ 切换正常,左侧物品列表 + 筛选渲染(非白屏) | ✅ 通过 | 见截图 3 |
| 三个 iframe 启动时全部加载(display 切换,状态保留) | ✅ 通过 | iframeCount=3 始终存在 |
| 右上「☰ 选择工具」按钮返回门页 | ✅ 通过 | toggle 点击后 gate=flex |

**证据截图**(人物卡 / 法术卡 / 物品卡工具渲染):
- 人物卡:`C:\Users\Administrator\.zcode\cli\artifacts\sess_0ca629cd-e987-4096-913b-fdd6401e361d\call_f06a75a0554a4d12b5a7da41-tool-result-491f2259-c719-4bef-9eac-6b9f405fce37.png`
- 法术卡:`C:\Users\Administrator\.zcode\cli\artifacts\sess_0ca629cd-e987-4096-913b-fdd6401e361d\call_5e11e55c9d1f4d36974dd6ab-tool-result-2e560c57-ec0d-4dd5-9778-db269a97ede5.png`
- 物品卡:`C:\Users\Administrator\.zcode\cli\artifacts\sess_0ca629cd-e987-4096-913b-fdd6401e361d\call_ec550a93590e42109d8f1828-tool-result-b12b2b2f-ac94-479f-8eb8-8d4babecfacd.png`

> ⚠️ 实测局限:IAB 对 blob iframe(`src=blob:`)为跨源,`contentDocument` 不可读、内部交互(点选法术/生成角色/导出)无法在浏览器工具内探查。故深层交互(如兼职等级分配器、AC 计算、长法术截断)仍以**源码静态走查**判定,与第 1 轮一致。

---

## 1. 与第 1 轮清单对比:逐项状态

### 1.1 已确认仍存在的问题(第 1 轮标记未修,本轮源码核对仍未修)

- **P1-1** 兼职"主职业等级"调整器 `setMainLevels` 只拦 `mcTotal<0`,`mcTotal===0` 放行 → 兼职变 0 级。**(仍存,最高优先)**
- **P1-2** 多个兼职按比例再分配有损,求和可能超 `mcTotal` 或最后一项为负。**(仍存)**
- **P2-1** AC 粗估且与实际装备脱钩(重甲硬编码 16+2)。**(仍存)**
- **P2-2** 起始装备不可选,角色表 dump 选项原文。**(仍存)**
- **P2-3** 法师「法术书 vs 已准备」混淆。**(仍存)**
- **P2-4** 子职法术未结构化。**(仍存)**
- **P2-5** 战技 `MANEUVERS` 20 条 `d:""` 全空,详情按钮退回"请查阅规则书"。**(仍存)**
- **P3-1** 法术数据含「奇械师」但 `CLASSES` 无该职业 → 搜索误导。**(仍存)**
- **P4-1** 法术卡 `.card-desc{overflow:hidden}` 硬裁长描述(已加 size=3 兜底,但裁切机制仍在)。**(仍存)**
- **P4-2** 打印件无页码(`.page-label` 在 `@media print` 隐藏)。**(仍存)**
- **P4-3** 角色名不上打印件。**(仍存)**
- **P5-1** 拖拽排序仅 HTML5 drag,触屏不可用。**(仍存)**
- **P6-1** 拼音搜索依赖 CDN,离线/被拦静默退化,用户无提示。**(仍存)**

### 1.2 本轮新确认 / 补充的问题

- **P4-4(新细化)** 物品卡多段描述 `\n` 处理:`cardHtml` 用 `.replace(/\n/g,"<br>")`,但物品卡 `.card-desc` 的 `white-space` 未像法术书那样设为 `pre-wrap`。两段式描述(如含 `\n` 的药水)在卡上分段可能丢失或多余空白被折叠。第 1 轮已提及,本轮对照两工具 CSS 确认差异仍在。
- **P3-3** `_serve.js` 文件清单曾列出但源码目录实际不存在(构建/说明引用与实际不一致)。**(仍存,提示性)**

### 1.3 第 1 轮已标记 ✓ 修复,本轮源码再次确认保持修复状态

C1 重载还原(`go(S.step)`)、C2 19 级传奇恩惠(`ASI_LEVELS` 含 19 + epic 分支)、C3 专长属性计入(`asiBonusTotal`)、C4 健壮/矮人 HP 三源判定、C5 木精灵速度/卓尔黑视(`effSpeed`/`effDarkvision`)、C6 掷点去重(`arrayUsed`)、C7 阵营邪恶三项(下拉含三项)、C8 技能去重(Set)、C9/C10/C11 豁免/技能加值与被动察觉(角色表已显示)、C17/C18 打印样式与 `break-inside`、C20 XSS 转义(`esc()` 覆盖关键字段)、C21 键盘可达(MutationObserver 补 role/tabindex + Enter/Space)、A1 切换按钮让位(embedded class 留 130px)、A2 天赋法术贯通(`grantedSpells`+`broadcastGranted`+监听)、B1 材料说明上卡(`compText` 拼接 materials)。

---

## 2. 按工具的未修问题归位

### 2.1 人物卡(冒险者创建指南)

- P1-1 兼职主职业等级调整器可设兼职 0 级(最高优先)
- P1-2 多项兼职再分配有损
- P1-3 战士/游侠子职施法者等级提示缺失
- P2-1 AC 粗估 / P2-2 起始装备不可选 / P2-3 法师书 vs 准备 / P2-4 子职法术未结构化 / P2-5 战技描述空
- P3-1 奇械师数据不对齐
- P5-2 一键排序选项单一
- P7-1/7-2/7-3 转义不统一 / 全局函数无测试 / `renderSheet` 巨型单函数

### 2.2 法术卡(法术书生成)

- P4-1 长描述被 `overflow:hidden` 硬裁
- P4-2 打印无页码 / P4-3 角色名不上打印件
- P5-1 拖拽触屏不可用
- P6-1 拼音搜索静默退化

### 2.3 物品卡(魔法物品卡)

- P2-6 `displayType` 解析依赖 meta 格式,易退化(旧 显示问题清单 问题 3/7)
- P3-3 `_serve.js` 缺失(提示性)
- P4-4 多段描述换行处理与法术卡不一致
- P5-1 拖拽触屏不可用
- 自定义/空白卡:旧"显示问题清单"问题 1(空白卡不空白)、9(自定义无法标消耗品/充能)经源码核对——`makeCustom` 已不再写死(consumable/charges 由表单 `readForm` 透传),**这两项实际已随 8/31 修复落地**(表单已有 cCons/cCharges 输入)。

---

## 3. 优先级建议(基于本轮实测 + 源码核对)

1. **P1-1 / P1-2**:兼职等级分配器边界 bug(角色数据错误,最高)。
2. **P4-1**:法术卡长描述裁切(打印成品可用性)。
3. **P2-1 / P2-2**:AC 与起始装备(角色卡核心字段失真)。
4. **P3-1**:奇械师数据/职业不对齐(搜索误导)。
5. **P2-5**:战技描述为空(详情按钮无效)。
6. **P5-1**:触屏不可用(移动端换序无路径)。
7. **P6-1**:拼音搜索静默退化(用户无感知)。
8. **P4-2 / P4-3**:打印页码与角色名(成品完整性)。
9. **P7-1**:转义/注入路径统一(防御性)。

---

## 4. 与第 1 轮相比的净变化

- 新增实测维度:线上主站加载/切换/iframe 渲染全部 ✅(第 1 轮因环境限制完全无法实测)。
- 旧"物品卡显示问题清单"中**问题 1(空白卡不空白)、9(自定义无法标消耗品/充能)** 经源码核对已随近期修复落地。
- 其余未修项与第 1 轮一致,**无新增严重 Bug**,整体代码质量稳定;待修项集中在"数据完整性 + 触屏交互 + 打印成品细节"。
