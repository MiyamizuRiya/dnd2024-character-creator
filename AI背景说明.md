# D&D 2024 工具站源码合集 · AI 背景说明

> 本文件给 AI 助手阅读，帮助快速理解这套项目是什么、数据从哪来、怎么跑、有哪些坑。人类也可读。

## 一、这是什么

一套**纯静态**（HTML + CSS + JS，无构建、无后端、无 npm）的 D&D 2024 中文工具站，双击 `.html` 即可用（`file://` 直接运行；联网时部分功能更佳）。共 4 个相关站点：

| 站点 | 入口 | 说明 |
|---|---|---|
| **法术书生成** | `法术书生成/法术书生成.html` | 搜索/筛选法术 → 勾选 → 右侧实时排版成可打印法术卡（每页 3 行 × 2 列 = 6 张）。数据 `data/spells.js`（391 条，玩家手册2024 法术详述 0~9 环）。 |
| **魔法物品卡** | `魔法物品卡/index.html` | 同款的魔法物品卡排版站。数据 `data/items.js`（419 条，城主指南2024 魔法物品详述）。支持消耗品标注、充能圆圈、多张同物品、空白自定义卡。 |
| **冒险者创建指南** | `DND2024冒险者创建指南.html` | 单文件自包含。分步生成 1 级冒险者角色卡。用 `localStorage` 存状态。 |
| **DND 工具合集** | `DND工具合集.html` | 把「法术书生成」+「冒险者创建指南」整页内联进**一个**自包含 html，用 blob-URL 双 iframe 隔离，右上角金色 ⇄ 切换。= 部署版。 |

`netlify-deploy/index.html` 与 `DND工具合集.html` **内容相同**（前者是改名为 index.html 的部署副本，用于 Netlify 拖拽部署）。

## 二、数据来源

均来自中文 5e 参考站 **https://5echm.kagangtuya.top**（站名「5E 不全书」，WinCHM 转 HTML，JS 生成的目录树 + 嵌套 frame 页）：

- **法术**：`玩家手册2024/法术详述/0环.htm … 9环.htm`（10 页，h4+<p> 结构）。
- **魔法物品**：`城主指南2024/7.宝藏/魔法物品详述/{护甲,药水,戒指,卷轴,武器,法杖,魔杖,权杖,奇物[装饰品/其他物品/着装品]}/{稀有度}.htm`。
  - ⚠️ 注意：站点目录树(TOC)里穿戴类奇物的链接写的是 `着装物品/`，**那是失效旧路径（404）**；真正能访问的是 **`着装品/`**（少一个「物」字）。当初为此漏抓了 47 件穿戴物品，后用 `着装品/` 正确路径补齐。
  - 多种稀有度的物品（治疗药水/巨人之力药水/魔咒护甲/艾恩石/异能塑像 等）在源站是一个带表格的条目；已**展开成各变体独立卡片**（如治疗药水 → 4 张：普通/非普通/珍稀/极珍稀）。
- **冒险者创建指南**：原源文件在 `C:\Users\<user>\.zcode\workspace\default\DND2024冒险者创建指南.html`（单文件自包含）；本合集里的副本是从那里复制的。

## 三、技术要点

- **纯静态、零依赖**：所有站都是原生 HTML/CSS/JS，无框架、无构建。`file://` 双击即跑；也兼容 http 部署。
- **打印 = 浏览器打印**：点「打印为 PDF」调 `window.print()`，CSS `@media print` 只显示卡片网格，每页固定 6 张（A4/Letter），用户在打印对话框选「另存为 PDF」。
- **过长卡片自动占多格**：离屏测量每张卡自然高度 → 超 1 格占 2 格(2 行 1 列)，超 2 格占 3 格(整列)；`computeSizes`+`packPages` 做 2×3 网格装箱，绝不重叠/溢出。
- **法术书搜索含拼音层**：在线时从 jsdelivr CDN 动态 `import('pinyin-pro')`，支持同音异字（「摩登肯」→「魔邓肯」）。离线则退化成字符级模糊（仍支持缺字/调序，如「匕首云」→「匕首之云」）。**物品站未接拼音层**（仅子串匹配）。
- **合集(DND工具合集.html)**：两个工具各 base64 成 blob URL 塞进 iframe，互不干扰；切换钮在 shell 层 fixed 右上角。
- **奇幻主题**：羊皮纸/皮革/金色；环阶/稀有度各有配色。

## 四、文件结构

```
DND工具站源码合集/
├─ AI背景说明.md            ← 本文件
├─ DND工具合集.html         ← 合集(自包含, 部署版)
├─ DND2024冒险者创建指南.html  ← 冒险者指南(自包含源)
├─ netlify-deploy/index.html ← = DND工具合集.html 的部署副本(index.html 命名)
├─ 法术书生成/
│   ├─ 法术书生成.html       ← 入口(引用 css/js/data)
│   ├─ css/styles.css
│   ├─ data/spells.js        ← window.SPELL_DATA = [391 法术]
│   ├─ js/app.js             ← 全部交互逻辑
│   └─ 问题清单.md           ← 早前一份代码审查(部分已修)
└─ 魔法物品卡/
    ├─ index.html            ← 入口
    ├─ css/styles.css
    ├─ data/items.js         ← window.ITEM_DATA = [419 物品]
    ├─ js/app.js
    └─ 显示问题清单.md        ← 物品站代码审查(大部分已修)
```

法术/物品数据字段：`id, nameZh, nameEn, level/category, rarity, school/type, detail, classes/attunement, meta, description, source`；物品额外有 `consumable`(消耗品) 和 `charges`(充能数)。

## 五、功能清单（两个卡片站共有，物品站更全）

搜索/筛选（类别/稀有度/同调）· 整行点选 · 多张同物品 · 置顶↑/一键排序⇅/拖拽排序 · 导入导出 md（可读列表 + 内嵌 JSON，按 id+名称回退）· 空白自定义卡（表单填写）· 消耗品徽章 · 充能圆圈(≤20 圆 / >20 文字) · 顶栏按稀有度计数 · A4/Letter + 缩放。

法术书额外：拼音模糊搜索（在线）、角色名(写入导出文件名)。
物品站额外：消耗品识别、充能识别。

## 六、已知坑 / 环境注意

1. **开发环境 Git Bash 崩**：原开发机 Git Bash 因 `STATUS_DLL_INIT_FAILED`（疑似杀软注入 `msys-2.0.dll`）完全不可用，所以**没有 shell**；所有文件操作走 Node `fs`（在 ZCode 的 node_repl 里）。要复现/改数据需用 Node 或修好 bash。
2. **合集的拼音层**：blob iframe 来源是 `null`，远程 CDN `import` 在某些浏览器/离线时会被拦 → 退化成字符级模糊（不影响主功能）。
3. **真机渲染未实测**：开发期间内置浏览器(IAB)的截图/DOM 抓取坏了，只能静态校验（语法/数据/装箱单测），未做肉眼渲染核对。逻辑层均过单测。
4. **着装品路径**：见二节——TOC 给的 `着装物品` 是 404，真路径是 `着装品`。若要重新抓取穿戴类物品，用 `着装品/{稀有度}.htm`。
5. **多种稀有度物品**：源站是「一个条目 + 一个变体表格」；展开逻辑见各 `app.js`（命名变体按行拆、环阶变体按稀有度合并、+1/+2/+3 按 meta 拆、散文变体(艾恩石/异能塑像)按 <li>/<strong> 拆并补共用说明）。

## 七、怎么用 / 怎么改

- **用**：双击入口 html（法术书生成.html / 魔法物品卡/index.html / DND工具合集.html）。打印按右上「打印为 PDF」。
- **部署**：把 `netlify-deploy/index.html`（或 `DND工具合集.html` 改名 index.html）拖到 Netlify 站点的 Deploys 拖放区，或 push 到已连接的 GitHub 仓库（站点已连 `github.com/MiyamizuRiya/dnd2024-character-creator`，push main 自动部署到 `create-your-dnd2024-pc.netlify.app`）。
- **改数据**：编辑 `法术书生成/data/spells.js` 或 `魔法物品卡/data/items.js`（`window.SPELL_DATA`/`window.ITEM_DATA = [...]`）。重新抓取需浏览器自动化（5echm 是 JS 渲染的 frame 站，不能用普通 curl/WebFetch）。
- **改样式**：`css/styles.css`（奇幻配色在 `:root` 变量；稀有度配色在 `--r-*` / `--l*`）。
- **改逻辑**：`js/app.js`（IIFE，无全局污染；选择模型用 `selEntries [{uid,itemId}]` 支持多张）。

## 八、审查文档说明

- `法术书生成/问题清单.md`、`魔法物品卡/显示问题清单.md`：早前对源码的纯阅读审查，列出了若干显示/数据问题。**其中大部分已修复**（同调布尔、空白卡、多段描述、需同调徽章、超长占 3 格、自定义充能/消耗品等）；剩余几条是无害/低优先（见各 md 末尾）。读这两份时请对照「已修」状态，勿当未修处理。

## 九、一句话总结

四套 D&D 2024 中文静态工具站（法术卡 / 物品卡 / 角色卡 / 合集），数据从 5echm 抓取并清洗，纯前端、打印成 PDF、奇幻主题、功能完备（搜索/多选/自定义卡/导入导出/装箱排版）。要扩展主要改 `data/*.js`（数据）和 `js/app.js`（逻辑）。
